<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="org.hello.ibatis.bean.StudentMapper">  
    <select id="selectbyid" parameterType="int" resultType="org.hello.ibatis.bean.UserInfo">
    	select * from userinfo where id = #{id}
    </select>
</mapper>
